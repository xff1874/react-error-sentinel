const transform = require('../command/transform');

const add = (a, b) => a + b;

describe('read and rewrite CRE templates', () => {
    beforeEach(() => {
        // do something
    });

    test('1+1=2', () => {});
});
